{
  "name": "temporal-splendor-demo",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-prod": "NODE_ENV=production browserify index.js | uglifyjs -mc > bundle.js",
    "build": "browserify index.js > bundle.js",
    "watch": "watchify index.js -o bundle.js -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Russel Simmons <russ@rsimmons.org>",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^10.2.0",
    "watchify": "^3.2.1"
  }
}
